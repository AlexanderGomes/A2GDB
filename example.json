{
    map[BACKEND_OP:SELECT refList:map[$0:UserId $1:Username $2:Age $3:City
        ] rels: [map[id: 0 inputs: [] relOp:LogicalTableScan table: [User
                ]
            ] map[condition:map[op:map[kind:EQUALS name:= syntax:BINARY
                    ] operands: [map[op:map[kind:CAST name:CAST syntax:SPECIAL
                            ] operands: [map[input: 0 name:$0
                                ]
                            ] type:map[nullable: true type:BIGINT
                            ]
                        ] map[literal: 6.759883095902072e+18 type:map[nullable: false type:BIGINT
                            ]
                        ]
                    ]
                ] id: 1 relOp:LogicalFilter
            ] map[id: 2 relOp:LogicalProject selected_columns: [Username City
                ]
            ]
        ]
    ]
}